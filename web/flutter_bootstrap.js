{{flutter_js}}
{{flutter_build_config}}

// Le service worker Flutter généré est volontairement désactivé : la PWA
// utilise uniquement web/sw.js, qui gère aussi les notifications Web Push.
_flutter.loader.load();
