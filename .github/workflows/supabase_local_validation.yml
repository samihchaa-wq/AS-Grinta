name: Supabase local full-stack validation

on:
  pull_request:
    branches: [main]
    paths:
      - 'supabase/**'
      - 'scripts/ci/assert_supabase_local_only.sh'
      - 'scripts/ci/run_supabase_local_validation.sh'
      - 'scripts/ci/normalize_local_migration_versions.py'
      - 'scripts/ci/generate_historical_match_fixtures.py'
      - 'scripts/ci/supabase_legacy_baseline.sql'
      - 'scripts/ci/supabase_legacy_match_status_text.sql'
      - 'scripts/ci/supabase_legacy_postmatch.sql'
      - 'scripts/ci/supabase_legacy_coach_tables.sql'
      - 'scripts/ci/supabase_legacy_analytics_views.sql'
      - 'scripts/ci/supabase_legacy_claim_transition.sql'
      - 'scripts/ci/supabase_legacy_post_goals_views.sql'
      - 'scripts/ci/supabase_legacy_named_roster_transition.sql'
      - 'scripts/ci/supabase_legacy_match_location.sql'
      - 'scripts/ci/supabase_legacy_season_rpcs.sql'
      - '.github/workflows/supabase_local_validation.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: supabase-local-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  SUPABASE_TELEMETRY_DISABLED: '1'
  DO_NOT_TRACK: '1'

jobs:
  local-only-guard:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Reject remote-capable commands and credentials
        run: bash scripts/ci/assert_supabase_local_only.sh

  supabase-local:
    needs: local-only-guard
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: 2.81.3

      - name: Install SQL and JSON clients
        run: |
          sudo apt-get update
          sudo apt-get install --yes postgresql-client jq rsync

      - name: Prepare CI-only migration compatibility
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p ci-artifacts/supabase-local
          rm -f supabase/migrations/*_ci_legacy_*.sql
          cp scripts/ci/supabase_legacy_match_status_text.sql \
            supabase/migrations/20260708000350_ci_legacy_match_status_text.sql
          cp scripts/ci/supabase_legacy_baseline.sql \
            supabase/migrations/202607080004_ci_legacy_baseline.sql
          cp scripts/ci/supabase_legacy_postmatch.sql \
            supabase/migrations/20260710105000_ci_legacy_postmatch.sql
          cp scripts/ci/supabase_legacy_coach_tables.sql \
            supabase/migrations/20260710115000_ci_legacy_coach_tables.sql
          cp scripts/ci/supabase_legacy_analytics_views.sql \
            supabase/migrations/20260710135000_ci_legacy_analytics_views.sql
          python3 scripts/ci/generate_historical_match_fixtures.py \
            supabase/migrations/20260710152000_restore_historical_match_scores.sql \
            supabase/migrations/20260710151000_ci_legacy_historical_matches.sql \
            | tee ci-artifacts/supabase-local/historical-fixture-generation.log
          cp scripts/ci/supabase_legacy_claim_transition.sql \
            supabase/migrations/20260710205000_ci_legacy_claim_transition.sql
          cp scripts/ci/supabase_legacy_post_goals_views.sql \
            supabase/migrations/20260711110050_ci_legacy_post_goals_views.sql
          cp scripts/ci/supabase_legacy_named_roster_transition.sql \
            supabase/migrations/20260712135000_ci_legacy_named_roster_transition.sql
          cp scripts/ci/supabase_legacy_match_location.sql \
            supabase/migrations/20260713165000_ci_legacy_match_location.sql
          cp scripts/ci/supabase_legacy_season_rpcs.sql \
            supabase/migrations/20260713192500_ci_legacy_season_rpcs.sql
          python3 scripts/ci/normalize_local_migration_versions.py \
            supabase/migrations \
            | tee ci-artifacts/supabase-local/migration-version-normalization.log
          count=$(find supabase/migrations -maxdepth 1 -type f \
            -name '*_ci_legacy_*.sql' | wc -l)
          test "$count" -eq 11

      - name: Run isolated Supabase validation
        run: bash scripts/ci/run_supabase_local_validation.sh

      - name: Stop and erase local Supabase
        if: always()
        shell: bash
        run: |
          set +e
          work="${RUNNER_TEMP}/as-grinta-supabase-local-${GITHUB_RUN_ID}"
          if [ -d "$work" ]; then
            cd "$work"
            supabase stop --no-backup >/dev/null 2>&1 || true
          fi
          rm -rf "$work"
          rm -f "${GITHUB_WORKSPACE}"/supabase/migrations/*_ci_legacy_*.sql \
            "${RUNNER_TEMP}/supabase-local-status.env" \
            "${RUNNER_TEMP}/ci-normal-signup.json" \
            "${RUNNER_TEMP}/ci-admin-signup.json" \
            "${RUNNER_TEMP}/supabase-start.raw.log"

      - name: Upload sanitized Supabase logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: supabase-local-validation-${{ github.run_id }}
          path: ci-artifacts/supabase-local
          if-no-files-found: warn
          retention-days: 7

  flutter-validation:
    needs: local-only-guard
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Read application version
        shell: bash
        run: |
          set -euo pipefail
          version=$(awk '/^version:/{print $2}' pubspec.yaml)
          test -n "$version"
          echo "APP_VERSION=$version" >> "$GITHUB_ENV"

      - name: Analyze
        shell: bash
        run: |
          set -o pipefail
          flutter analyze --fatal-infos 2>&1 | tee analyze.log

      - name: Test
        shell: bash
        run: |
          set -o pipefail
          flutter test 2>&1 | tee test.log

      - name: Build Web release
        shell: bash
        run: |
          set -o pipefail
          flutter build web \
            --release \
            --base-href "/AS-Grinta/" \
            --pwa-strategy=none \
            --dart-define-from-file=config/production.json \
            --dart-define=APP_VERSION="$APP_VERSION" \
            2>&1 | tee build-web.log

      - name: Upload Flutter logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: supabase-local-flutter-${{ github.run_id }}
          path: |
            analyze.log
            test.log
            build-web.log
          if-no-files-found: warn
          retention-days: 7
