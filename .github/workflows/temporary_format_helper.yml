name: Temporary Dart format helper

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  format:
    if: github.head_ref == 'agent/simplify-match-workflows'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - name: Install locked dependencies
        run: flutter pub get --enforce-lockfile
      - name: Format changed Dart files
        run: |
          dart format \
            lib/features/auth/presentation/auth_state.dart \
            lib/features/sports_management/presentation/admin_squad_plan_page_widgets.dart \
            test/predictions_controller_test.dart
      - name: Upload formatted files
        uses: actions/upload-artifact@v4
        with:
          name: formatted-final-dart-files
          path: |
            lib/features/auth/presentation/auth_state.dart
            lib/features/sports_management/presentation/admin_squad_plan_page_widgets.dart
            test/predictions_controller_test.dart
