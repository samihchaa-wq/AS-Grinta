name: Capture Flutter analyze

on:
  push:
    branches:
      - agent/jauges-neon-ballon-v2

permissions:
  contents: write

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/jauges-neon-ballon-v2
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - run: flutter pub get
      - name: Capture analyze output
        run: flutter analyze > analysis-output.txt 2>&1 || true
      - name: Remove temporary workflow
        run: rm .github/workflows/analyze-agent.yml
      - name: Commit diagnostic
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add analysis-output.txt .github/workflows/analyze-agent.yml
          git diff --cached --quiet || git commit -m "Capturer le diagnostic Flutter"
          git push origin HEAD:agent/jauges-neon-ballon-v2
