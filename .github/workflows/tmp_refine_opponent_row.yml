name: Temporary refine opponent row

on:
  push:
    branches:
      - feat/reorganize-match-form-sections

permissions:
  contents: write

jobs:
  refine:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: feat/reorganize-match-form-sections
      - name: Bootstrap
        run: echo ready
