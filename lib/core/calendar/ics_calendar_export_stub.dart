Future<void> downloadIcsFile({
  required String contents,
  required String filename,
}) async {
  throw UnsupportedError(
    'L’export de calendrier est disponible dans la version Web/PWA.',
  );
}
